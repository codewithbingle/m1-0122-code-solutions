/* exported getLastChar */

// getLastChar(string)

// Before Pseudo Code
// define a function named getLastChar, which has a single parameter named 'string'
// define a variable named lastIndex and assign it to the length of the string minus one
// define a variable named lastCharacter and assign it to the string index lastIndex
// return the value of lastCharacter

function getLastChar(string) {
  var lastIndex = string.length - 1;
  var lastCharacter = string[lastIndex];
  return lastCharacter;
}

// After Pseudo Code
// define a function named getLastChar, which has a single parameter named 'string'
// define a variable named lastIndex and assign it to the length of the string minus one
// define a variable named lastCharacter and assign it to the string index lastIndex
// return the value of lastCharacter

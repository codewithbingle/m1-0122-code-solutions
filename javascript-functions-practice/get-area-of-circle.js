/* exported getAreaOfCircle */
// 6

function getAreaOfCircle(radius) {
  var areaOfCircle = Math.PI * (radius * radius);
  return areaOfCircle;
}

// var areaOfCircleResult = getAreaOfCircle(8);

// console.log('The are of a circle with the given radius is: ', areaOfCircleResult);

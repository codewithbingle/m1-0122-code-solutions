/* exported getPropertyValue */
// 22

function getPropertyValue(object, key) {
  var propertyValueResult = object[key];
  return propertyValueResult;
}

/* var bingle = {
  name: 'Bingle Padilla',
  occupation: 'Software Engineer',
  birthPlace: 'Philippines'
}; */

// eslint-disable-next-line no-console
// console.log('The value stored in the object at the specified property key is: ', getPropertyValue(bingle, 'occupation'));

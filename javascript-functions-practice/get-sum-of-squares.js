/* exported getSumOfSquares */
// 5

function getSumOfSquares(x, y) {
  var sumSquared = (x * x) + (y * y);
  return sumSquared;
}

// var getSumOfSquaresResult = getSumOfSquares(3, 8);

// console.log('The sum of x squared and y squared is: ', getSumOfSquaresResult);

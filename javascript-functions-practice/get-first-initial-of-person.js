/* exported getFirstInitialOfPerson */
// 20

function getFirstInitialOfPerson(person) {
  var firstInitialOfPerson = person.firstName;
  var firstInitialResult = firstInitialOfPerson[0];
  return firstInitialResult;
}

// var bingle = { firstName: 'Bingle ', lastName: 'Padilla' };

// console.log('The first character of the string stored in the firstName property of the person object is: ', getFirstInitialOfPerson(bingle));

/* exported head */

// 1 head(array)

// Before Pseudo Code
// define a function named head, which has a single parameter 'array'
// define a variable named firstElement and assign it to the parameter with index at zero
// return the value of firstElement

function head(array) {
  var firstElement = array[0];
  return firstElement;
}

// After Pseudo Code
// define the function named head, which has a single parameter 'array'
// define a variable named firstElement and assign it to the parameter 'array' index at zero
// return the variable firstElement;

/* exported last */

// 2 last(array)

// Before Pseudo Code
// define the function named last, which has a single parameter 'array'
// define a variable named lastIndex and assign it to array.length minus 1
// define a variable named lastElement and assign it to array index lastIndex
// return lastElement

function last(array) {
  var lastIndex = array.length - 1;
  var lastElement = array[lastIndex];
  return lastElement;
}

// After Pseudo Code
// define a function named last, which has a single parameter 'array'
// define a variable named lastIndex and assign it to array.length minus 1
// define a variable named lastElement and assign it to array index lastIndex
// return the values lastElement

/* exported setValue */

// 2 setValue(object, key, value)

// Before Pseudo Code
// define a function named setValue, which has three parameters 'object', 'key', 'value'
// return nothing

function setValue(object, key, value) {
  object[key] = value;
}

// After Pseudo Code

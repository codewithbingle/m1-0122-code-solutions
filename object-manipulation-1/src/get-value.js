/* exported getValue */

// 1 getValue(object, key)

// Before Pseudo Code
// define a function named getValue, which has two parameters 'object' and 'key'
// return the value of the object index at key

function getValue(object, key) {
  return object[key];
}

// After Pseudo Code
// define a function named getValue, which has two parameters 'object' and 'key'
// return the value of the object index at key
